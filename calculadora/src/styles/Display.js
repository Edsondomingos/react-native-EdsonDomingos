import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    display: {
        flex: 1,
        backgroundColor: '#fff',
        // fontSize: 100,
        // fontWeight: 'bolder',
        justifyContent: 'flex-end',
        alignItems: 'flex-end',
    },
})