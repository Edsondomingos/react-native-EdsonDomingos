import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    cabecalho: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
        color: '#f00',
    },
  });